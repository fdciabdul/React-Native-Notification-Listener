import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flexDirection: "column",
        alignSelf: "center",
    },
    text : {
        fontSize: 20,
        fontWeight: "bold",
        alignContent : "center",
        marginTop: '100%'
    }
});